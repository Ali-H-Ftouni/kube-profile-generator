# Registry (Docker Hub) — Login + convention de nommage

Ce projet pousse les images sur **Docker Hub**.

## 1) Pré-requis
- Docker installé sur la machine
- Un compte Docker Hub
- Une image déjà buildée en local (STEP 4)

Vérifier Docker :
```bash
docker --version
